server:
  port: 6222

spring:
  application:
    name: @artifactId@
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS-HOST:discovery-server}:${NACOS-PORT:8848}
        password: nacos
        username: nacos
  security:
    user:
      name: admin
      password: password
